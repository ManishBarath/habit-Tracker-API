# Backend Environment Variables Template
# Commit this file to GitHub for reference.
# You can use these in Render as Environment Variables.

# Database Connection (Neon/Supabase)
ConnectionStrings__DefaultConnection=Host=my-postgres-host;Database=postgres;Username=myuser;Password=mypassword;SslMode=Require;

# API Settings
ASPNETCORE_ENVIRONMENT=Production
